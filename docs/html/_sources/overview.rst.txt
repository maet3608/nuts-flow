Overview
========

Sinks
-----

Sinks aggregate input elements into a single output.

- :class:`Collect <nutsflow.sink.Collect>` :
  collect input elements in a container, e.g. list, dict.

- :class:`Sum <nutsflow.sink.Sum>` :
  compute sum of input elements.
